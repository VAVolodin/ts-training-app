import React, { FC } from 'react';
import RegistrationPage from '../../Components/RegistrationPage';

const RegPageContainer: FC = () => {
    return (
        <>
            <RegistrationPage />
        </>
    );
};

export default RegPageContainer;
